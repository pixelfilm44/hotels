/* Deployment config.
   `server`: WebSocket URL of the online game server. Used only when this page
   is served statically (GitHub Pages / file://); when the page is served by
   the Node server itself, the same origin is used automatically. */
window.HOTELS_CONFIG = {
  server: 'wss://hotels-pixel-tycoon.onrender.com'
};
